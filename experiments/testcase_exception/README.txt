This testcase relies on speed of threads (assumes that they are the same). So the code usually works, however JPF can find the bug.


Execute the ./go_good.sh sript in order to get the correct implementation.
Execute the ./go_bad.sh in order to inject an error.
